
# Rahul Shetty Academy Playwright Tests

This workspace now uses Playwright Test (JavaScript) for the requested test.

Install dependencies:

```bash
npm install
npx playwright install
```

Run the test:

```bash
npx playwright test tests/login.spec.js
```

If you want to run all tests:

```bash
npx playwright test
```
